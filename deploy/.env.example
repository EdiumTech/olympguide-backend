API_DOMAIN=api.olympguide.ru
DATA_ROOT=/srv/olympguide/data
RELEASE_ID=local
# Generate with: python deploy/deploy.py prepare
DB_PASSWORD=
REDIS_PASSWORD=
MINIO_PASSWORD=
TOKEN_SECRET=
SESSION_SECRET=
BEARER_DATA_LOADER_TOKEN=

# Optional: registration/password reset mail only works with real SMTP settings.
# Set COMPOSE_PROFILES=email after adding the credentials.
COMPOSE_PROFILES=
SMTP_SERVER=smtp.mail.ru
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
